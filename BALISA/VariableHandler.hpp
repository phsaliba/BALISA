#pragma once

#include <map>
#include <string>
#include <vector>

using std::map;
using std::string;
using std::vector;

map<string, double> create_trasported_var_map(vector<string> input_transported_vars);